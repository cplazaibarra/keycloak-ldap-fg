# Servidor RADIUS - Keycloak con plugin RADIUS
# Formato: server[:port] shared_secret timeout (seconds)
#
# El usuario se autenticará con: password + OTP_code (todo junto)
# Ejemplo: si password=cplazapassword123 y OTP=123456
# entonces se envía: cplazapassword123123456

192.168.122.151 fortigateradiussecret 10

# Descomentar para permitir autenticación local como fallback
# default_realm = master
